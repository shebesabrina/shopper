<h1>Categories <%= @categories.count %></h1>
<ul>
    <% @categories.each do |category|%>
        <li>
            <%= link_to category.title, category_products_path(category) %>
            (<%= category.products.count %> products)
        </li>
    <%end%>
</ul>